export const configs = {
  // APPAREANCE
  theme: 'default',

  // SCREEN
  brightness: 100,
  brightnessAuto: false,
  nightMode: false,
  suspensionTime: 5, // in minutes

  // STATUS BAR
  clockPosition: 'right', // 'right', 'center'
  devBadge: true,
}
